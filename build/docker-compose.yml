version: "3"

services:
  bp-server:
    build:
      context: ../
      dockerfile: build/Dockerfile
    ports:
      - 50051:50051
    environment:
      - BP_LOGIN_LIMIT=10
      - BP_PASSWORD_LIMIT=100
      - BP_IP_LIMIT=1000
